#pragma once

#include "../lib1/lib1.h"

/* This struct declaration is expanded from a macro defined in lib1, so its
 * source location is a macro location and must be resolved to the expansion
 * location (this file) when assigning it to a directory package. */
DECLARE_T30011_STRUCT(D);
